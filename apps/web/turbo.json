{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".nitro/**", ".tanstack/**", ".vercel/**"]
    },
    "lint:type-check": {
      "outputs": ["node_modules/.tmp/**"]
    }
  }
}
